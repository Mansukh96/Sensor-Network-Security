#include<stdio.h>
#include<string.h>
#include<stdint.h>


// const to place array in rom instead of rom
const uint8_t sbox[16*16] = {	//declare static
   
};

const uint8_t invsbox[16*16] = {	//declare static
   
};

uint8_t rcon[10] = {0x01, 0x02, 0x04, 0x08, 0x10, 0x20, 0x40, 0x80, 0x1B, 0x36};


typedef uint8_t state_t[4][4];
state_t* state;	//declare static


uint8_t RoundKey[176];	//declare static

const uint8_t* key;	//is ptr required? declare static



void keyExpansion(void) {	//declare static
	uint8_t i,j,k, temp[4];

	for (i=0; i<4; i++) {
		RoundKey[i*4 + 0] = key[i*4 +0];
		RoundKey[i*4 + 1] = key[i*4 +1];
		RoundKey[i*4 + 2] = key[i*4 +2];
		RoundKey[i*4 + 3] = key[i*4 +3];
	//instead of loop
	}

	for (; i<44; i++) {
		for (j=0; j<4; j++)
			temp[j] = RoundKey[(i-1)*4 + j];


		if (i%4==0) {

			//function rotWord()
			{
			k=temp[0];
			temp[0]=temp[1];
			temp[1]=temp[2];
			temp[2]=temp[3];
			temp[3]=k;
			//instead of loop
			}

			//function subWord()
			{
			temp[0] = sbox[temp[0]];
			temp[1] = sbox[temp[1]];
			temp[2] = sbox[temp[2]];
			temp[3] = sbox[temp[3]];
			//instead of loop
			}


			temp[0] = temp[0] ^ rcon[i/4 - 1];
		}


		{
		Roundkey[i*4 + 0] = Roundkey[(i-4)*4 + 0] ^ temp[0];
		Roundkey[i*4 + 1] = Roundkey[(i-4)*4 + 1] ^ temp[1];
		Roundkey[i*4 + 2] = Roundkey[(i-4)*4 + 2] ^ temp[2];
		Roundkey[i*4 + 3] = Roundkey[(i-4)*4 + 3] ^ temp[3];
		//instead of loop
		}

}



void AddRoundKey(uint8_t round) {	//declare static
	uint8_t i,j;
	for (i=0; i<4; i++)
		for (j=0; j<4; j++)
			(*state)[i][j] ^= Roundkey[round*16 + i*4 +j];
}


void SubBytes(void) {		//declare static
	uint8_t i,j;
	for (i=0; i<4; i++)
	for (j=0; j<4; j++)
		(*state)[i][j] = sbox[(*state)[i][j]];
}


void ShiftRows(void) {	//declare static
	uint8_t temp;

	temp = (*state)[1][0];
	(*state)[1][0] = (*state)[1][1];
	(*state)[1][1] = (*state)[1][2];
	(*state)[1][2] = (*state)[1][3];
	(*state)[1][3] = temp;

	temp = (*state)[2][0];
	(*state)[2][0] = (*state)[2][2];
	(*state)[2][2] = temp;
	temp = (*state)[2][1];
	(*state)[2][1] = (*state)[2][3];
	(*state)[2][3] = temp;

	temp = (*state)[3][0];
	(*state)[3][0] = (*state)[3][3];
	(*state)[3][3] = (*state)[3][2];
	(*state)[3][2] = (*state)[3][1];
	(*state)[3][1] = temp;

	//instead of following loop
	/*
	uint8_t i, j, temp, repeat;
	for (i=1; i<4; i++) {
		for (repeat=0; repeat<i; repeat++) {
			temp = state[i][0];
			for (j=0; j<4; j++)
				state[i][j] = state[i][j+1];
			state[i][3] = temp;
		}
	}
	*/
}



uint8_t gadd(uint8_t a, uint8_t b) {
	return a ^ b;
}


/* Multiply two numbers in the GF(2^8) finite field defined 
 * by the polynomial x^8 + x^4 + x^3 + x + 1 = 0
 * using the Russian Peasant Multiplication algorithm
 * (the other way being to do carry-less multiplication followed by a modular reduction)
 */
uint8_t gmul(uint8_t a, uint8_t b) {
	uint8_t p = 0; /* the product of the multiplication */
	while (b) {
            if (b & 1) /* if b is odd, then add the corresponding a to p (final product = sum of all a's corresponding to odd b's) */
                p ^= a; /* since we're in GF(2^m), addition is an XOR */

            if (a & 0x80) /* GF modulo: if a >= 128, then it will overflow when shifted left, so reduce */
                a = (a << 1) ^ 0x11011b; /* XOR with the primitive polynomial x^8 + x^4 + x^3 + x + 1 -- you can change it but it must be irreducible */
            else
                a <<= 1; /* equivalent to a*2 */
            b >>= 1; /* equivalent to b // 2 */
	}
	return p;
}




static uint8_t xtime(uint8_t x)
{
  return ((x<<1) ^ (((x>>7) & 1) * 0x1b));
}

// MixColumns function mixes the columns of the state matrix
static void MixColumns(void)
{
  uint8_t i;
  uint8_t Tmp,Tm,t;
  for(i = 0; i < 4; ++i)
  {  
    t   = (*state)[i][0];
    Tmp = (*state)[i][0] ^ (*state)[i][1] ^ (*state)[i][2] ^ (*state)[i][3] ;
    Tm  = (*state)[i][0] ^ (*state)[i][1] ; Tm = xtime(Tm);  (*state)[i][0] ^= Tm ^ Tmp ;
    Tm  = (*state)[i][1] ^ (*state)[i][2] ; Tm = xtime(Tm);  (*state)[i][1] ^= Tm ^ Tmp ;
    Tm  = (*state)[i][2] ^ (*state)[i][3] ; Tm = xtime(Tm);  (*state)[i][2] ^= Tm ^ Tmp ;
    Tm  = (*state)[i][3] ^ t ;        Tm = xtime(Tm);  (*state)[i][3] ^= Tm ^ Tmp ;
  }
}

// Multiply is used to multiply numbers in the field GF(2^8)
#if MULTIPLY_AS_A_FUNCTION
static uint8_t Multiply(uint8_t x, uint8_t y)
{
  return (((y & 1) * x) ^
       ((y>>1 & 1) * xtime(x)) ^
       ((y>>2 & 1) * xtime(xtime(x))) ^
       ((y>>3 & 1) * xtime(xtime(xtime(x)))) ^
       ((y>>4 & 1) * xtime(xtime(xtime(xtime(x))))));
  }


// MixColumns function mixes the columns of the state matrix.
// The method used to multiply may be difficult to understand for the inexperienced.
// Please use the references to gain more information.
static void InvMixColumns(void)
{
  int i;
  uint8_t a,b,c,d;
  for(i=0;i<4;++i)
  { 
    a = (*state)[i][0];
    b = (*state)[i][1];
    c = (*state)[i][2];
    d = (*state)[i][3];

    (*state)[i][0] = Multiply(a, 0x0e) ^ Multiply(b, 0x0b) ^ Multiply(c, 0x0d) ^ Multiply(d, 0x09);
    (*state)[i][1] = Multiply(a, 0x09) ^ Multiply(b, 0x0e) ^ Multiply(c, 0x0b) ^ Multiply(d, 0x0d);
    (*state)[i][2] = Multiply(a, 0x0d) ^ Multiply(b, 0x09) ^ Multiply(c, 0x0e) ^ Multiply(d, 0x0b);
    (*state)[i][3] = Multiply(a, 0x0b) ^ Multiply(b, 0x0d) ^ Multiply(c, 0x09) ^ Multiply(d, 0x0e);
  }
}


void encrypt(void) {
	uint8_t round;

	AddRoundKey(0);

	for (round=1; round<10; round++) {
		SubBytes();
		ShiftRows();
		MixColumns();
		AddRoundKey(round);
	}

	SubBytes();
	ShiftRows();
	AddRoundKey(10);

}
